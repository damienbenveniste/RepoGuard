# Repo-local Codex defaults for ScaffoldGuard.

approval_policy = "on-request"
sandbox_mode = "workspace-write"
allow_login_shell = false
project_doc_max_bytes = 32768

[features]
hooks = true
multi_agent = true

[agents]
max_threads = 3
max_depth = 1
job_max_runtime_seconds = 1800
