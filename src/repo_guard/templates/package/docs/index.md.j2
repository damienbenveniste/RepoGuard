# {{ project_slug }}

`{{ project_slug }}` is a strict Python package generated by `repo-guard`.

## Validate

```bash
uv run repo-guard validate
```

## Explore

Start with `src/{{ package_name }}/core.py` and `tests/unit/test_core.py`.
